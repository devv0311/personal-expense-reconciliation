/**
 * Bank-statement CSV adapter.
 *
 * One synthetic source format, read into source values and nothing more. See `parse.ts` for
 * what this adapter is and is not allowed to know.
 */

export * from './parse.js';
