# Generated build output and dependencies.
dist/
coverage/
node_modules/

# Generated by drizzle-kit; formatting it would fight the generator on every regeneration.
drizzle/meta/

# Scratch state written by a local tooling plugin, not project source. Matches the
# corresponding entry in eslint.config.js.
.remember/

# web/ is a standalone Next.js package with its own Prettier config (double quotes, matching
# create-next-app's own convention rather than fighting it) and its own format/format:check
# scripts (ADR-0042, corrected — checking it against this root config was tried first and
# immediately disagreed with every file create-next-app generated).
web/

# Browser-verification artifacts — a driven browser writes these, Prettier does not own them.
.playwright-mcp/

# The capability audit's own evidence (report, matrix, API inventory, screenshots). It is a
# record of one external review, quoted throughout the work that closed its findings —
# reformatting it would edit the evidence.
audit-results/
