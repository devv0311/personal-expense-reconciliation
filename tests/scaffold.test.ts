import { describe, expect, it } from 'vitest';

/**
 * This is a placeholder proving the test harness (TypeScript + Vitest + strict mode)
 * is wired correctly end to end. Delete it once real domain tests exist — see
 * docs/testing/testing-strategy.md for what those must cover before any financial
 * arithmetic ships.
 */
describe('repository scaffold', () => {
  it('runs TypeScript tests through Vitest', () => {
    expect(1 + 1).toBe(2);
  });
});
